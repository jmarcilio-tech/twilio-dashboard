# csv-proxy — allowlist

Adicionar à lista de ficheiros permitidos / fetch em paralelo:

- `conf_usage_billing_by_category.csv`

Mesmo host/raw base que `conf_usage_billing_snapshot.csv`.

Se o proxy devolver um JSON com vários CSVs em texto, incluir chave opcional, por exemplo:

`usageBillingByCategoryText` ou `conf_usage_billing_by_category`
