/**
 * Fundir no vosso use-twilio-data.ts (exemplo de estado + resposta).
 */

// type TwilioCsvBundle = {
//   ...
//   deliveryHorario: DeliveryHorarioRow[];
//   deliveryInsightsTimeseries: DeliveryInsightsTimeseriesRow[];
// };

// Após obter texto do proxy:
// const deliveryInsightsTimeseries = rawTimeseries
//   ? parseDeliveryInsightsTimeseriesCsv(rawTimeseries)
//   : [];

// Expor no return do hook: { ..., deliveryInsightsTimeseries }
